# MediaPipe Python Tasks 핵심 패키지
mediapipe==1.0.0

# OpenCV 카메라/이미지 입출력. mediapipe 의존성과 맞추기 위해 contrib 판 사용.
opencv-contrib-python

# 이미지 배열/수치 계산
numpy

# 시각화/일부 MediaPipe 내부 설정 파일 생성 경로와 관련
matplotlib

# DINOv3 기반 미세표정 변화 heatmap
Pillow
transformers>=4.56,<6

# 오디오 실험을 확장할 때 사용할 패키지
soundfile

# --- ON DAMM video analyzer ---
# 아래 핀은 기존 핀을 변경하지 않는 추가(additive) 섹션이다.
torch>=2.7,<2.14
ultralytics>=8.4,<9
insightface==0.7.3
emotiefflib[torch]>=1.1,<2
opencv-python>=4.10,<5
scikit-learn>=1.4

# 테스트 러너
pytest>=8,<10
